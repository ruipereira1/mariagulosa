{
  "name": "maria-gulosa",
  "version": "1.0.0",
  "type": "module",
  "description": "Site da Maria Gulosa - Bolos artesanais",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "vercel dev",
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "deploy-dev": "vercel",
    "deploy-auto": "npm run build && vercel --prod",
    "preview": "cd frontend && npm run preview",
    "install-deps": "npm install && cd frontend && npm install",
    "clean": "cd frontend && rm -rf dist node_modules && npm install"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "keywords": ["bolos", "confeitaria", "maria-gulosa"],
  "author": "Maria Gulosa",
  "license": "MIT"
} 